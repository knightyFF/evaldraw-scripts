Some image manipulation algorithms experiments. They are rather old. If they don't compile use ken's Eval compiler instead of Rscript.
To load a picture press (ctrl)(p) then recompile by pressing (alt)(enter).
